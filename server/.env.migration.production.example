# Supply this value through the deployment secret store. The migration job must
# receive only the database credential, not OAuth, JWT, or encryption secrets.
DATABASE_URL=mysql://senderwho:replace-me@private-mysql:3306/senderwho?sslaccept=strict
